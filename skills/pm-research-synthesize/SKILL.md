---
name: pm-research-synthesize
description: 将用户访谈、问卷、客服反馈、运营记录、可用性测试、会议纪要和开放文本等多源研究材料转化为可追溯的产品洞察，通过材料盘点、编码归类、用户分群、场景链路、主题与反例分析、证据强度评估，输出问题机会、关键假设和后续产品或研究动作。适用于研究综合和反馈归因；不用于简单摘要、用少量个案代表整体或脱离原始材料直接给功能方案。
---

# pm-research-synthesize

把分散材料综合为能够支持产品判断的洞察，而不是按文档顺序做摘要。

## 优先读取
- 所有需要留档或交接的产物都遵守[产物修订记录与版本追溯规范](../../shared-references/产物修订记录与版本追溯规范.md)；纯临时草稿可不建立修订表。

1. [研究综合模板](./references/研究综合模板.md)
2. 需要参考完整粒度时，再读[研究综合示例](./references/研究综合示例.md)

## 与需求定义的关系

- 可独立用于研究总结，也可在 `pm-requirement-define` 缺少用户证据时按需调用。
- 输出可追溯的证据、主题、反例和机会，不替代产品目标、方案方向和一级范围决策。
- 研究结论形成后，将证据强度、适用人群和局限交回 `pm-requirement-define`；不是每个需求的固定前置步骤。

## 输入检查

开始前说明：

- 本次研究问题或希望支持的决策
- 材料类型、数量、时间和来源
- 样本用户、角色、场景和招募方式
- 已知偏差、缺失人群和数据限制

如果材料不是为同一问题收集，先分组，不要强行合并成单一结论。

## 证据原则

- 每条洞察都能回溯到材料、样本或记录编号
- 区分用户原话、行为观察、业务事实和分析解释
- 频次不等于重要度，强烈情绪不等于普遍性
- 主动寻找反例、沉默人群和与主流结论冲突的材料
- 引用用户原话时匿名化，并只保留支持判断的短片段
- 不根据研究材料推断无法覆盖的整体市场比例

## 工作流程

### 1. 建立材料清单

记录材料编号、来源、用户类型、场景、日期、质量和可回答的问题。排除重复、无关或明显失真的材料，并说明原因。

### 2. 提取证据单元

以一个完整观察为单位提取：

- 用户 / 角色和场景
- 目标任务
- 行为、原话或事件
- 阻碍、动机或结果
- 材料编号
- 初始标签

不要直接把整段访谈总结成抽象结论。

### 3. 编码与聚类

优先按“用户—场景—任务—问题环节—当前替代—理想状态”编码，再将相似证据聚成主题。避免仅按功能模块分类而丢失用户任务。

### 4. 分析用户差异

比较不同用户类型、使用频率、生命周期、渠道、角色和任务场景。明确哪些主题是共性，哪些只适用于特定分群。

### 5. 分析主题、反例和根因

每个主题说明：

- 支持证据和覆盖样本
- 问题发生的流程环节
- 用户影响和当前替代方式
- 可能原因
- 反例、矛盾信息和其他解释

### 6. 评估证据强度

使用三级标准：

- `high`：多种来源或多个分群反复出现，行为与表达一致，少量反例不改变主结论
- `medium`：在一个主要分群内重复出现，或多来源提供部分支持
- `low`：孤立个案、材料质量有限、存在明显矛盾或只能作为假设

证据强度不等于问题优先级；优先级还需考虑影响程度、目标相关性和解决成本。

### 7. 转化为产品机会

将洞察写成：

```text
对于【用户/角色】，在【场景/任务】中，
因为【阻碍/原因】，难以达到【理想状态】，
当前通过【替代方式】解决，并付出【成本/风险】。
```

基于机会提出下一动作：进入需求定义、补充定量数据、追加访谈、可用性测试或低成本原型验证。不要从一条洞察直接跳到唯一功能方案。

## 产物修订记录与版本追溯

只要本次输出会写入项目目录、被引用、交接或供他人评审，必须在产物中保留当前版本和修订记录。第一次留档写 `v0.1`；后续每轮改变规则、范围、证据、状态、验收、审计或发布事实时立即追加一条。纯错别字和不改变语义的排版可不记录。

修订记录至少说明：版本、日期、触发原因 / 阶段、主要变化、受影响章节或下游、确认人 / 责任人、状态（`active` / `superseded` / `stale` / `pending-confirmation` / `archived`）。上游版本变化时，不得静默覆盖旧结论；若影响当前产物，标记旧版本或旧证据失效并重新审查。

## 质量门禁

本 skill 的“完成标准”只表示产物已按方法生成，不等于已通过交接质量审计。

- 讨论、探索或用户明确要求“只先给初稿”时，不自动审计，并明确标记“未审计”；
- 当输出达到 `ready`、给出最终分级、准备交给设计 / 研发 / 测试 / 运营，或用户要求复核 / 验收时，必须在本次交付前自动调用 `pm-quality-audit`；
- 只有 `PASS`，或用户明确接受带责任人和完成条件的 `PASS_WITH_CONDITIONS`，才可标记为可交接；
- `BLOCKED` 必须回到本 skill、证据 skill 或上游决策，不得用补造事实的方式消除；
- 是否需要把审计报告写入项目根目录 `quality-audits/`，按用户是否要求留档 / 交接附件决定。

## 默认输出

1. 研究问题与一句话结论
2. 材料、样本和方法局限
3. 用户与场景分布
4. 关键主题、证据和强度
5. 分群差异、反例和矛盾信息
6. 用户任务链路与问题机会
7. 关键假设和不能下的结论
8. 产品建议、研究建议和优先级
9. 证据索引

## 交给需求定义的交接契约

研究综合完成后至少交回：

- 研究问题和一句话证据结论
- 关键证据、样本覆盖和证据强度
- 适用用户 / 场景、分群差异和反例
- 不能下的结论、材料局限和证据缺口
- 对问题、目标、方向或范围的影响
- 建议进入需求定义、补数据、追加研究或低成本验证的下一步

不要把研究建议直接写成唯一产品方案；如果仍存在方向选择，交给 `pm-requirement-define` 统一决策。

## 完成标准

- 每条关键洞察能追溯到材料编号
- 用户原话、行为事实和分析解释明确区分
- 共性、分群差异和反例均被呈现
- 证据强度与业务优先级分别判断
- 建议说明要解决的问题和下一验证方式
- 明确材料无法支持的结论

需要基于洞察定义目标、方向和范围时，交给 `pm-requirement-define`。
- 持久化产物已标注当前版本、状态和修订记录；重要变更能说明影响范围与是否需要下游重做。
