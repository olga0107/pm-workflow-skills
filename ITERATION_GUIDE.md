# 使用反馈与迭代指南

这套 skills 应通过真实需求持续演进，但不要因为一次不满意输出就不断堆规则。迭代目标是减少重复人工修改，同时保持触发准确、职责清楚和跨项目可复用。

## 目录

1. 什么时候值得改
2. 每次反馈需要保留什么
3. 先判断应该改哪一层
4. 与 Codex 协作的推荐方式
5. 修改后的回归验证
6. 建议节奏与版本记录

## 1. 什么时候值得改

优先处理：

- 选错 skill，或应触发时没有触发
- 多个 skills 重复执行同一工作
- 输出缺少研发、测试或决策所需的关键信息
- 模板导致稳定的重复、空章节或过度填写
- 事实、假设、结论和待确认项经常混在一起
- 同类任务连续出现相同人工修改

谨慎处理：

- 单个项目独有的业务规则或文档路径
- 只出现一次的表达偏好
- 可以通过用户当次指令解决、且没有复用价值的问题
- 为了覆盖极少数场景而显著增加所有任务的上下文和步骤

一般建议：阻塞性问题立即修；非阻塞问题累计 3 至 5 个真实案例后批量判断。

## 2. 每次反馈需要保留什么

建议为有价值的问题保留一张“迭代反馈卡”：

```markdown
## Skill 迭代反馈

- 使用的 skill：
- 任务类型和输入成熟度：
- 原始输入或脱敏摘要：
- 实际输出或问题片段：
- 期望结果：
- 哪些内容必须保留：
- 人工修改了什么：
- 影响程度：阻塞 / 明显返工 / 轻微体验
- 是否重复出现：首次 / 已出现 N 次
- 适用范围：通用 / 某类需求 / 单个项目
```

涉及用户数据、内部指标或未公开方案时，只保留脱敏片段和结构性问题。

## 3. 先判断应该改哪一层

| 问题类型 | 优先修改位置 | 例子 |
|---|---|---|
| 触发错误 | `SKILL.md` front matter、`agents/openai.yaml`、命令入口 | “写更新日志”错误触发 PRD 或 `pm-requirement-define` 没有先分级 |
| Skill 选择或交接不清 | README、WORKFLOW_GUIDE、相关 skills 的边界和输入输出契约 | 竞品分析结束后没有回到需求定义，或需求定义未先进入分级 |
| 执行步骤反复出错 | `SKILL.md` 工作流程和完成标准 | 总是先写方案、后补问题 |
| 输出结构不友好 | 对应 `references/` 模板 | PRD 章节重复或缺少原型关联 |
| 输出深度不足 | 模板、示例和质量门 | 状态、权限或验收无法支持实现 |
| 页面内容不完整 | 共享参考、需求定义 / 分级交接、PRD / HTML 模板和案例 | 原型只能评估模块骨架，静态文案、状态标签、空态或页面密度滞后到设计稿 |
| 多个 skills 共用的原则不一致 | `shared-references/`、README、WORKFLOW_GUIDE | 六问、指标、责任边界或分级门禁口径不一致 |
| 业务规则或工具路径只在一个项目有效 | 具体项目的 `AGENTS.md`、README 或 docs | Figma 文件命名、业务状态枚举 |
| 单次特殊要求 | 当次提示词 | 只为一次汇报调整语气 |

遵循最小修改原则：能改模板解决，就不扩大 skill 流程；能放项目文档，就不写进全局 skill。

## 4. 与 Codex 协作的推荐方式

可以直接把反馈卡、原始输出文件或问题截图交给 Codex，并明确希望采用哪种协作模式：

### 仅审查

```text
请审查这次使用 pm-prd-write 的结果，判断问题属于触发、流程、模板、项目上下文还是单次执行偏差。先给修改建议，不直接改文件。
```

### 审查并修改

```text
这是一次真实使用反馈。请先判断是否值得沉淀为通用规则；如果值得，修改最小必要文件，同步模板和示例并运行校验。如果只适用于当前项目，不要写入全局 skill。
```

### 累计案例复盘

```text
这里有 N 个真实使用案例。请归类重复问题，区分高频系统问题和一次性偏好，再提出一轮 skills 迭代方案。不要按案例逐条堆规则。
```

每次协作最好明确“哪些表现不好”和“哪些现有能力不能被改坏”，比只说“再优化一下”更容易形成稳定改进。

## 5. 修改后的回归验证

至少用三类案例验证：

1. 目标案例：原问题是否真正改善。
2. 相邻案例：类似但复杂度不同的任务是否仍正常。
3. 反向案例：不应该触发或不应该展开该内容的任务是否保持克制。

重点观察：

- Skill 是否选对
- 是否出现职责越界或重复工作
- 人工删除和补写量是否下降
- 输出是否仍支持下一角色继续工作
- 可选章节是否按实际风险触发
- 原型、功能、验收和指标能否追溯
- 页面内容契约中的静态 / 动态 / 条件 / 反馈内容是否完整，原型是否真正呈现当前故事的用户可见页面，而不是只画模块示意
- 是否引入新的冗余、空表或虚构内容

### 深度质询触发的专项回归

修改 `pm-requirement-define` 的质询判断后，至少用三类样本回归：

1. 明确不应质询：已确认方案写 PRD、低风险文案调整、只缺异常规格、可从项目资料查到的事实。
2. 应先补证据：用户流失原因未知、只凭竞品都有就要做、缺少规模或效果数据。
3. 应质询：一句话功能想法、方向和范围未定、多方业务原则冲突、AI / 自动化责任未定。

记录四项结果：

- 触发准确率：被建议质询的案例中，质询是否确实影响或确认了产品决策。
- 关键问题召回：重大未决决策是否被识别。
- 误触发率：本可直接进入需求分级、PRD 或证据 skill，却被拉入质询的比例。
- 严重漏判：资金、权限、AI 责任、合规等重大问题是否漏过。

不要只看总体准确率；在真实案例不足时，不声称固定准确率。优先保证严重风险不漏判，再用单问题试探和用户确认降低普通需求的误触发成本。预置案例见 `skills/pm-requirement-define/references/深度质询触发案例.md`。

修改后运行仓库校验，并检查 Markdown 链接、YAML、命名一致性、工作流主线和变更差异。涉及页面体验时，额外检查需求定义 → 分级 → PRD 内容契约 → HTML 内容覆盖的交接是否完整。

## 6. 建议节奏与版本记录

- 阻塞实现或导致错误决策的问题：发现后立即修复。
- 高频返工问题：累计多个案例后按主题迭代。
- 纯表达偏好：优先通过当次提示或项目规则处理。
- 每轮结构性调整更新 `CHANGELOG.md`，说明解决的问题、改变的边界和可能影响的旧用法。
- 在形成稳定使用量后，可按发布批次打 Git tag；不需要为每次文案微调单独升级版本。

建议长期关注四个结果：skill 选择准确度、人工返工量、交付信息缺失率和输出冗余度。它们比“文档是否更长”更能判断迭代是否有效。
