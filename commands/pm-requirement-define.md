# pm-requirement-define

把模糊想法、用户问题或零散提需推进为可进入需求分级与后续交付路由的产品需求定义。

## 使用方式

1. 调用 `pm-requirement-define`
2. 读取可查项目事实，区分证据、产品决策、产品规格和研发实现缺口
3. 判断是否不需要质询、只需一个试探问题，或建议用户确认后进入深度质询
4. 还原现状事实、用户、场景和影响
5. 识别证据缺口，按需使用研究综合、竞品分析或指标复盘，不默认全部执行
6. 定义问题、目标与非目标
7. 方向未定时比较真实可选方案
8. 划定一级范围并扫描会影响方向或投入的重大约束
9. 如果涉及页面改版或设计前 UX 评审，记录页面效果评审目标、内容完整性要求、静态预览限制和不可由设计稿补齐的信息；不展开页面规格，但把“需要页面内容契约 / UX 原型 / 静态 HTML 交付”作为交给 `pm-requirement-grade` 的体验交付意图和风险线索
10. 输出 `ready`、`needs-decision` 或 `needs-input`

达到 `ready` 后先转 `pm-requirement-grade` 做 L0/L1/L2/L3/H 分级；只有分级结论要求 PRD 时再转 `pm-prd-write`。

用户可以明确说“挑战一下这个方案”或“进入深度质询”；未明确要求时，skill 应先说明判断理由和预计问题范围，不静默开始多轮追问。
