interface:
  display_name: "pm-update-write"
  short_description: "将已确认变化整理成更新说明、变更通知或结构化更新日志"
  default_prompt: "使用 $pm-update-write 将这些已确认变化整理成清晰的更新说明或更新日志。 交付前：若输出 ready、最终结论或准备交接，自动调用 pm-quality-audit；若只是讨论或初稿，则标记为未审计。"

policy:
  allow_implicit_invocation: true
