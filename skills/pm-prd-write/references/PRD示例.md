# PRD 示例：活动详情页提前说明圈内优先报名资格

> 虚构示例，用于展示精简后的章节结构、原型关联和追溯粒度。业务规则、文件路径和指标不可直接用于真实项目。

## 目录

0. 文档信息与一页结论
1. 决策依据
2. 方案总览、体验结构与原型
3. 交付范围与功能需求
4. 规则、数据与质量边界
5. 影响、依赖与发布
6. 验收与追溯
7. 效果验证
8. 风险、待确认项、研发 spec 交接与附录

## 本示例的章节适用性

本示例保留全部章节，不代表所有 PRD 都必须如此：

| 章节 | 为什么本例需要 |
|---|---|
| 0、1、3、6 | 核心必填章节 |
| 2. 方案总览、体验结构与原型 | 涉及客户端页面、两类用户状态和三个设计资产 |
| 4. 规则、数据与质量边界 | 涉及资格状态、兜底、埋点、性能和可访问性 |
| 5. 影响、依赖与发布 | 涉及客户端兼容、服务端依赖和灰度放量 |
| 7. 效果验证 | 需要验证无效点击和客服咨询是否下降 |
| 8. 风险、待确认项、研发 spec 交接与附录 | 存在字段依赖、理解风险、补充资料和技术方案交接 |

如果是单页面静态文案调整，通常可以省略第 4、5、7、8 章；如果是纯服务端规则修复且没有页面或流程变化，可以省略第 2 章。省略原因应记录在 0.1。

## 0. 文档信息与一页结论

### 0.1 文档信息

| 字段 | 内容 |
|---|---|
| 需求名称 | 活动详情页提前说明圈内优先报名资格 |
| 文档状态 | 评审中 |
| 负责人及参与角色 | 产品：李明；设计、客户端、服务端、测试、运营、数据 |
| 目标版本 | v3.8.0 |
| 创建与更新时间 | 2026-07-13 |
| 需求定义来源 | `pm-requirement-define`：圈子活动资格理解问题 |
| 输出形态 | 标准 PRD；可按需摘出评审简版 |
| 关联资料 | 客服工单分析、报名漏斗、活动详情原型、服务端资格规则 |
| 省略章节及原因 | 无；本例用于展示所有条件章节的完整写法 |

### 0.2 一页结论

- 一句话方案：在圈子活动详情页报名区，根据用户身份提前展示优先报名资格，减少无效报名尝试和资格咨询。
- 已确认目标与方案：让用户点击前理解当前是否可报名；复用服务端现有资格判定，不改变报名资格规则。
- 关键取舍与非目标：本期不改活动列表、规则正文和名额分配机制。
- 本次交付范围与版本：iOS、Android 详情页、资格展示状态、相关埋点和 FAQ，随 v3.8.0 发布。
- 效果判断标准：资格限制返回率和资格咨询量下降，符合资格用户报名转化不明显受损。
- 用户主路径与页面影响：入口 → 活动详情 → 查看资格 → 报名 / 等待；本次只改活动详情页。
- 关键信息结构变化：在报名区域附近新增资格说明，活动最终状态仍优先展示。
- PRD / spec 边界：PRD 确认资格展示状态、页面表现、实时校验原则和验收；具体接口字段、状态枚举命名、兼容实现和监控实现交给研发 spec。
- 最大风险：详情展示状态与用户点击时的实时资格可能不一致。

### 0.3 修订记录

| 日期 | 版本 | 变化或决策 | 原因 | 负责人 |
|---|---|---|---|---|
| 2026-07-13 | 0.9 | 补充原型资产、状态兜底和灰度阈值 | 研发评审前闭环 | 李明 |

## 1. 决策依据

### 1.1 问题、目标与成功假设

| 问题与证据摘要 | 目标 ID / 目标状态 | 目标人群与场景 | 方向级成功假设 |
|---|---|---|---|
| 近四周资格相关咨询 128 条；圈外用户优先期点击后资格限制返回率 41% | OBJ-01：用户点击报名按钮前理解当前资格和开放条件 | 进入圈子活动详情页的圈内成员与圈外用户 | 提前解释资格会减少无效点击和咨询，不降低符合资格用户报名转化 |

### 1.2 已确认方向、范围与取舍

- 已确认方向：在详情页报名区展示个体化资格状态，并复用服务端现有身份与活动阶段判断。
- 选择依据：距离决策动作最近、可解释个人资格、无需改变资格规则。
- 上游未选方向：规则正文统一提示无法匹配个人身份；列表全链路提示投入较高且缺少入口侧证据。
- 一级范围：活动详情页报名链路、登录用户、圈内优先期和公开报名阶段。
- 非目标：不改变资格、名额、排序、通知和活动创建规则。
- 重大约束：页面提示不能代替报名接口实时校验；旧客户端必须继续可报名。

### 1.3 关键决策记录

| 决策项 | 已确认结论 | 依据 / 来源 | 决策人 | 日期 |
|---|---|---|---|---|
| 展示位置 | 放在详情页报名状态区域 | 最接近用户决策点 | 产品、设计 | 2026-07-10 |
| 资格主源 | 服务端报名接口仍为最终主源 | 防止身份和阶段变化 | 产品、服务端 | 2026-07-10 |

## 2. 方案总览、体验结构与原型

### 2.1 方案闭环与目标流程

页面加载时取得活动阶段、用户身份和报名状态，生成资格展示状态；用户阅读提示后决定等待或报名；点击报名时服务端再次实时校验。

| 场景 ID | 阶段 | 角色 / 系统 | 前置输入 | 动作 | 系统反馈 | 下一步 / 临界点 |
|---|---|---|---|---|---|---|
| SCN-01 | 进入详情 | 登录用户 / 客户端 | 活动、身份、报名状态 | 请求展示状态 | 返回最终状态、已报名、成员可报名、圈外等待、身份未知或普通报名状态 | 展示 UI-01 / UI-02 |
| SCN-02 | 点击报名 | 用户 / 服务端 | 实时活动与身份 | 提交报名 | 成功或资格限制 | 以实时结果为准 |

### 2.2 产品动线与页面影响

本次没有新增页面，页面地图整体不变；变化集中在活动详情页的资格信息和主操作状态。

| 动线 ID | 场景 ID | 入口 / 起点 | 用户目的与关键动作 | 页面 / 状态转移 | 关键分支 | 结果 / 出口 | 返回、失败、取消或重试 |
|---|---|---|---|---|---|---|---|
| FLOW-01 | SCN-01 | 活动列表、搜索、圈子页或外部链接 | 进入详情并判断能否报名 | 进入活动详情，展示资格状态 | 圈内 / 圈外；优先 / 公开阶段 | 点击报名或等待开放 | 无权限时展示“活动不可访问”；详情数据不完整时展示加载失败并提供“重新加载”；点击时允许实时重试 |
| FLOW-02 | SCN-02 | 活动详情 | 点击报名并获得结果 | 提交服务端实时校验 | 成功 / 资格失败 / 接口失败 | 报名结果或详情页反馈 | 业务失败留在详情页展示服务端原因；网络失败提示结果未确认并允许重试 |

### 2.3 页面信息结构

本次只列变化区域，不重复活动详情页既有全部字段。

```text
活动详情页
├── 活动基础信息区（保持不变）
├── 报名资格信息区（新增）
├── 主操作区（状态变化）
└── 活动规则与辅助信息区（保持不变）
```

| 页面 ID | 信息结构 ID | 区域目的 | 本次变化 | 展示条件 / 状态 | 数据 / 规则来源 |
|---|---|---|---|---|---|
| PAGE-01 活动详情 | IA-01 资格信息区 | 让用户在操作前理解当前资格 | 新增资格提示和开放时间 | 按活动最终状态、报名状态、活动阶段和用户身份展示 | RULE-01 至 RULE-03 |
| PAGE-01 活动详情 | IA-02 主操作区 | 引导用户执行可行操作 | 报名按钮或等待开放状态变化 | 最终活动状态优先 | RULE-03 |

### 2.4 页面内容契约

本例要求在设计前确认资格区和主操作区的页面效果，因此页面内容契约是用户可见内容主源：

| 内容 ID | 页面 / 信息区 | 用户可见内容 | 类型 | 展示条件 | 缺省 / 异常 / 恢复 | 长度 / 密度约束 | 原型 / 验收 |
|---|---|---|---|---|---|---|---|
| CONTENT-01 | PAGE-01 / IA-01 | 资格说明、开放时间、身份状态、不可报名原因 | 静态 + 条件 | 按活动状态、报名状态、阶段和身份展示 | 无法确认身份时说明原因并允许刷新；活动不可访问时不开放报名 | 资格说明不超过两行；原因就近展示 | UI-STATE-01 至 UI-STATE-05 / AC-01 至 AC-04 |
| CONTENT-02 | PAGE-01 / IA-02 | 报名按钮、等待开放、已报名、刷新和报名结果入口 | 条件 + 反馈 | 根据当前可执行动作展示 | 点击时实时校验；失败留在详情页并保留可恢复上下文 | 按钮使用具体动作，不使用“处理” | UI-STATE-01 至 UI-STATE-05 / FR-02 |

页面 × 状态原型必须呈现上述用户可见内容，不把“资格说明”“报名按钮”留给设计稿临时补齐；未确认文案或状态必须标记负责人和阻塞影响。

### 2.5 原型范围与页面状态规划

本例涉及页面状态和报名主操作，需要原型；页面容器不变，但报名区存在多个用户可见状态。原型由“页面 × 状态 × 内容”规划驱动，完整行为规则仍以第 3、4 章为准；确认后可交给 `pm-prd-html` 生成一份按阅读任务重组、完整规格可按需回看、带固定侧栏和黑白灰线框 SVG 的单文件 HTML。

| 原型状态 ID | 页面 ID | 状态名称 | 用户任务 | 需要表达的信息 / 反馈 | 主操作 | 下一状态 | 视口锚点（移动端） | 是否独立原型 | 关联功能 / 规则 / 验收 |
|---|---|---|---|---|---|---|---|---|---|
| UI-STATE-01 | PAGE-01 | 活动已关闭 / 已结束 | 判断是否还能报名 | 最终状态说明、不可报名原因 | 查看活动信息 / 返回 | 终止态 | 报名资格信息区 | 是 | FR-02 / RULE-01 / AC-04 |
| UI-STATE-02 | PAGE-01 | 圈内成员可优先报名 | 了解资格并报名 | 可优先报名说明、报名按钮 | 点击报名 | 报名成功 / 实时校验失败 | 报名资格和主操作区 | 是 | FR-02 / RULE-03、RULE-04 / AC-01 |
| UI-STATE-03 | PAGE-01 | 圈外用户等待公开报名 | 了解何时可以报名 | 等待公开报名时间、不可用按钮 | 等待 / 刷新 | 公开报名后可报名 | 报名资格和主操作区 | 是 | FR-02 / RULE-03 / AC-02 |
| UI-STATE-04 | PAGE-01 | 成员身份无法确认 | 理解当前不能确认的原因 | 暂无法确认提示、刷新或校验入口 | 刷新 / 点击报名校验 | 状态恢复 / 业务失败 | 报名资格和主操作区 | 是 | FR-02 / RULE-03、RULE-04 / AC-03 |
| UI-STATE-05 | PAGE-01 | 已报名 | 确认自己无需重复操作 | 已报名提示、结果入口 | 查看报名结果 | 已报名态 | 报名资格和主操作区 | 是 | FR-02 / RULE-02 / AC-04 |

独立原型判断：以上状态的可见信息、主操作或用户下一步存在实质差异，因此独立展示；普通报名状态沿用既有页面结构，不另造一套页面。

#### 移动端视口与滚动约束

- 端类型与逻辑视窗：沿用活动详情页现有 H5 页面形态；实际逻辑宽高待以已确认设计稿或实现页面为准。
- 页面长度与滚动：本需求只改动活动详情页中的资格信息区和主操作区；HTML 原型保留这些区域所处的页面上下文，不以缩小字号把完整详情塞进单一视窗。
- 固定区域：沿用已有详情页约定；没有来源时不新增吸顶或吸底操作。
- 状态视口锚点：以状态表中的报名资格和主操作区为准。
- 信息密度与可访问性：沿用既有活动详情页的字号、间距和触控习惯；设计资产缺失时，在 HTML 中标记待确认而非伪造高保真样式。

#### 原型约束

- 端类型与页面尺寸：沿用活动详情页现有 H5 页面形态；
- 目标用户：圈内成员、圈外用户和身份暂不可确认用户；
- 需要复用：活动详情页既有报名区、按钮和错误反馈形态；
- 不应暴露：`eligibility_display_state` 等内部状态枚举；
- 关键约束：文案只能说明用户当前能做什么和何时可以继续，不承诺名额；报名结果仍以点击时服务端校验为准。

### 2.6 原型与设计资产索引

| 资产 ID | 类型 | 精确链接 / 文件路径 / Frame | 版本或更新时间 | 状态 | 负责人 | 覆盖场景 / 页面 / 功能 |
|---|---|---|---|---|---|---|
| UI-01 | Figma | `活动详情页 / Frame: 圈内成员-可优先报名` | 2026-07-12 | 已确认 | 王华 | SCN-01 / FR-02 |
| UI-02 | Figma | `活动详情页 / Frame: 圈外用户-等待公开报名` | 2026-07-12 | 已确认 | 王华 | SCN-01 / FR-02 |
| UI-03 | 流程图 | `design/activity-priority-signup-flow.fig / Page: Main flow` | 2026-07-11 | 评审中 | 李明 | SCN-01、SCN-02 / FR-01 |

真实文档应把上述描述替换为可直接打开的具体 Frame / Node 链接或仓库文件路径。若需要单文件协作评审，`pm-prd-html` 以这些资产为参考，生成黑白灰内嵌 SVG 原型和流程图；Figma 仍可作为设计主源，不把 HTML 反向当作新的业务主源。

### 2.7 场景—原型关联

| 场景 ID / 流程步骤 | 功能 ID | 原型资产与节点 | 关键交互 / 状态 | 备注 |
|---|---|---|---|---|
| SCN-01 / 展示资格 | FR-02 | UI-01 | 圈内成员展示可报名提示，按钮可用 | 文案不承诺名额 |
| SCN-01 / 展示资格 | FR-02 | UI-02 | 圈外用户展示开放时间，按钮不可用 | 时间缺失使用兜底文案 |
| SCN-02 / 实时校验 | FR-01 | UI-03 / 报名校验节点 | 展示状态与实时结果冲突时以结果提示收口 | 无新增弹窗样式 |

## 3. 交付范围与功能需求

### 3.1 交付范围与版本

| 版本 | 目标 | 包含功能 / 页面 / 端 | 暂不包含 | 验证重点 |
|---|---|---|---|---|
| v3.8.0 | 提前解释资格 | iOS、Android 详情页；展示状态；埋点；FAQ | 列表、Web、资格规则 | 展示准确率和无效点击变化 |

### 3.2 功能清单

| 功能 ID | 功能点 | 所属页面 | 对应目标 / 场景 | 优先级 | 版本 | 原型 ID | 依赖 |
|---|---|---|---|---|---|---|
| FR-01 | 生成并返回资格展示状态 | PAGE-01 | OBJ-01 / SCN-01、SCN-02 | P0 | v3.8.0 | UI-03 | 身份与活动阶段数据 |
| FR-02 | 展示差异化资格提示 | PAGE-01 | OBJ-01 / SCN-01 | P0 | v3.8.0 | UI-01、UI-02 | FR-01 |
| FR-03 | 资格提示与报名埋点 | PAGE-01 | OBJ-01 / SCN-01、SCN-02 | P1 | v3.8.0 | 不适用 | 数据事件评审 |

### 3.2.1 系统能力需求表

本表从产品角度列出详情页需要的系统能力，不定义具体接口路径、请求 / 响应字段或实现方案。

| 能力 ID | 系统能力 | 支撑的页面 / 流程 | 触发时机 | PRD 需要确认的产品结果 | 失败或不可用时的用户兜底 | 关联功能 / 规则 | spec 需补齐 |
|---|---|---|---|---|---|---|---|
| CAP-01 | 资格展示状态判断能力 | PAGE-01 报名区 | 用户进入或刷新活动详情页 | 页面得到唯一可渲染的资格展示状态，且不由客户端本地推断资格 | 身份未知时展示不确定提示，不展示肯定性可报名 | FR-01、FR-02 / RULE-01 至 RULE-04 | API 字段、状态枚举命名、兼容实现 |
| CAP-02 | 报名实时校验能力 | 点击报名链路 | 用户点击可用报名按钮 | 点击时按最新活动、身份、报名和名额状态校验，结果覆盖详情旧状态 | 业务失败留在详情页展示原因；网络失败提示结果未确认 | FR-01、FR-02 / RULE-04 | 接口方法、错误码、幂等和并发策略 |
| CAP-03 | 提示曝光与报名结果串联能力 | 埋点与效果验证 | 提示进入可见区域、点击报名、收到结果 | 能判断提示是否降低无效点击和客服咨询 | 埋点失败不影响页面和报名 | FR-03 / MET-01 至 MET-03 | 事件字段、去重、看板和告警 |

### 3.3 功能需求明细

本节按照“需求结论 → 行为步骤 → 条件分支 → 异常与边界 → 追溯”编写。规则表和原型用于补充，不代替正文。

#### FR-01 生成资格展示状态

**需求结论**

登录用户每次请求圈子活动详情时，服务端同时返回一个唯一的资格展示状态，客户端直接按该状态渲染，不在本地重新计算资格。这个状态只用于解释当前页面应如何展示；用户真正点击报名时，报名接口必须再次根据最新活动阶段、成员身份、报名状态和名额情况校验，详情页状态不能直接作为报名凭证。

**行为说明**

1. 用户首次进入详情页、主动刷新页面或页面因其他操作重新请求详情数据时，服务端重新计算资格展示状态；页面停留期间不额外轮询。
2. 对圈子活动，服务端使用活动最终状态、当前报名阶段、用户是否已报名、用户圈内成员身份和公开报名时间参与判断，并在详情响应中返回唯一的 `eligibility_display_state`；非圈子活动不返回该新增状态字段，客户端继续使用既有报名状态渲染。
3. 客户端收到状态前，报名区域展示加载占位，不提前显示“可报名”或“不可报名”；收到状态后一次性替换为 FR-02 对应内容。
4. 用户点击可用的报名按钮时，客户端提交报名请求；服务端按点击时的最新数据重新校验。若实时结果与详情页展示不一致，以报名结果为准，页面随后刷新详情状态。

**状态判定与优先级**

| 优先级 | 进入条件 | 返回状态 | 状态含义 |
|---|---|---|---|
| 1 | 活动已取消、已结束或已超过报名截止时间 | `closed` | 当前不允许继续报名；具体结束原因由活动状态决定 |
| 2 | 活动仍可访问，当前用户已经报名成功 | `registered` | 用户无需再次报名 |
| 3 | 圈内优先报名阶段，用户为圈内成员 | `member_available` | 当前具备优先报名资格，但仍不代表名额锁定 |
| 4 | 圈内优先报名阶段，用户明确不是圈内成员 | `non_member_waiting` | 当前不能报名，等待公开报名 |
| 5 | 圈内优先报名阶段，但成员身份无法确认 | `identity_unknown` | 页面无法可靠判断资格，需要重试或在点击时实时校验 |
| 6 | 已进入公开报名阶段，且活动未关闭、用户未报名 | `normal_registration` | 不再区分圈内身份，用户可以提交报名 |

只要命中更高优先级状态，就不再计算低优先级状态。例如活动已截止时，即使用户是圈内成员，也必须返回 `closed`。

**异常与恢复**

| 异常条件 | 用户看到什么 | 系统 / 数据处理 | 恢复方式 |
|---|---|---|---|
| 详情请求整体失败 | 页面级错误提示“活动信息加载失败”，不展示报名按钮 | 不生成资格展示状态，不写入报名数据 | 用户点击“重新加载”后重新请求详情 |
| 仅成员身份获取失败 | 返回 `identity_unknown`，页面不展示肯定性资格结论 | 记录身份查询失败；不修改成员关系和报名状态 | 用户可刷新资格，也可点击报名交由报名接口实时校验 |
| 页面展示后活动阶段或身份变化 | 页面可能短暂保留旧展示 | 报名接口使用最新数据校验，不信任旧展示状态 | 返回明确报名结果，客户端刷新详情并更新展示 |

**保持不变与边界**

- 本功能不改变报名资格、成员关系、名额计算和报名结果规则，只增加详情页展示所需的派生状态。
- 非圈子活动不返回 `eligibility_display_state`，继续使用普通活动详情中的报名状态和既有报名按钮。
- 旧客户端忽略新增字段，仍通过报名接口获得最终结果。

**追溯：**PAGE-01 / IA-01、IA-02；UI-03；RULE-01 至 RULE-04；AC-01 至 AC-04。

#### FR-02 根据资格状态展示说明和报名操作

**需求结论**

活动详情页在活动基础信息与报名按钮之间增加资格说明区，让用户在点击前知道当前是否可报名、为什么以及下一步能做什么。资格说明、按钮状态和点击结果必须由同一个 `eligibility_display_state` 驱动，不能出现文案提示“可报名”但按钮不可用，或提示“等待开放”但按钮仍可提交的矛盾状态。

**页面行为与分支**

| 展示状态 | 资格说明区 | 主按钮与可操作性 | 用户操作后的结果 | 状态切换与优先级 |
|---|---|---|---|---|
| `member_available` | 显示“当前为圈内优先报名阶段，你可优先报名”；不使用“已获得名额”等承诺性文案 | 显示“立即报名”，可点击 | 点击后提交实时报名校验；成功进入报名结果页，失败留在详情页并展示服务端原因 | 活动关闭时切换为 `closed`；活动未关闭但用户已报名时切换为 `registered` |
| `non_member_waiting` | 有公开报名时间时显示“当前为圈内优先报名，MM月DD日 HH:mm 后开放报名”；无时间时显示“当前为圈内优先报名，公开报名后你可参与” | 显示“等待开放”，不可点击 | 不发起报名请求 | 到达公开报名阶段并刷新后切换为 `normal_registration`；活动关闭优先切换为 `closed` |
| `identity_unknown` | 显示“暂时无法确认你的报名资格，请刷新后重试”；提供“刷新资格”文字操作 | 显示“报名”，可点击，但不展示“可优先报名” | “刷新资格”重新请求详情；点击“报名”直接进入实时报名校验，成功或失败均以接口结果收口 | 身份恢复后切换为成员或非成员状态；活动关闭仍最高优先 |
| `normal_registration` | 不展示圈内优先说明，资格说明区整体收起 | 显示“立即报名”，可点击 | 点击后提交实时报名校验；成功进入结果页，失败留在详情页并显示原因 | 用户报名成功后切换为 `registered`；活动关闭切换为 `closed` |
| `registered` | 显示“你已报名”，不再展示资格限制说明 | 显示“已报名”，不可点击 | 不重复提交报名请求 | 活动关闭时页面同时展示活动最终状态，但保留已报名结果入口 |
| `closed` | 根据原因显示“报名已截止”“活动已结束”或“活动已取消” | 显示对应最终状态，不可点击 | 不发起报名请求 | 最高优先级，不再展示资格提示 |

**点击反馈与恢复**

1. 可点击按钮提交后立即进入加载状态并禁止重复点击，直到报名请求返回。
2. 报名成功时进入报名结果页；返回详情页后应展示 `registered`。
3. 因资格、活动阶段或名额变化失败时，用户停留在详情页，页面展示服务端返回的明确失败原因，并自动刷新详情状态；不得只提示“操作失败”。
4. 因网络错误或超时失败时，页面提示“网络异常，报名结果尚未确认，请重试”；按钮恢复可点击。客户端不得在未收到成功结果时展示已报名。
5. 用户离开后重新进入详情页，以新详情请求结果为准，不复用上次页面中的资格状态。

**保持不变与边界**

- 不修改活动列表卡片、活动规则正文、名额计算和消息提醒。
- 资格区只解释当前状态，不承诺最终名额；原型 UI-01、UI-02 只定义布局和视觉，以上行为与优先级以本节为准。

**追溯：**PAGE-01 / IA-01、IA-02；UI-01、UI-02；RULE-01 至 RULE-04；AC-01 至 AC-04。

#### FR-03 记录资格说明和报名链路事件

**需求结论**

埋点用于判断资格说明是否被真实看到、用户是否继续报名以及最终结果，不能影响页面展示和报名主流程。每条事件都必须带同一次页面访问和报名请求的关联标识，保证曝光、点击和结果可以组成漏斗，并避免页面重复渲染造成重复曝光。

**事件定义**

| 事件 | 精确触发时机 | 单次触发规则 | 必填参数 | 失败处理 |
|---|---|---|---|---|
| `priority_eligibility_hint_view` | `member_available`、`non_member_waiting` 或 `identity_unknown` 的资格说明实际进入可见区域时 | 同一 `page_view_id` 下，同一展示状态只记一次；状态变化后可记录新状态 | `page_view_id`、`activity_id`、`eligibility_display_state`、`activity_phase`、`membership_status`、`entry_source`、`client_version` | 上报失败不影响页面；由埋点 SDK 重试，不重复生成业务动作 |
| `activity_signup_click` | 用户点击可用报名按钮且客户端准备发起报名请求时 | 每次有效点击记一次；按钮加载期间的重复点击不记录 | 上述公共参数、`request_id`、按钮文案 | 上报失败不阻止报名请求 |
| `activity_signup_result` | 客户端收到该 `request_id` 对应的报名成功、业务失败或网络失败结果时 | 一个 `request_id` 只记一次最终结果 | 上述公共参数、`request_id`、`result`、`failure_code`；成功时 `failure_code` 为空 | 上报失败不改变报名结果；进入埋点错误日志 |

`membership_status` 只记录 `member`、`non_member`、`unknown`，不上传成员证明或其他身份原始数据。`result` 取值为 `success`、`business_failure`、`network_failure`；业务失败必须记录可聚合的 `failure_code`，不能只传自由文本。

**验收方式**

测试环境按一次页面访问执行“看到提示 → 点击报名 → 收到结果”，使用 `page_view_id` 和 `request_id` 核对三类事件能够串联；重复触发页面渲染不得产生第二条同状态曝光，连续点击被按钮防重拦截时不得产生额外点击事件。关闭或模拟埋点上报失败后，页面展示和报名结果必须保持正常。

**保持不变与边界**

- 不回填历史事件；旧客户端仍上报原有报名点击和结果事件，新参数允许为空。
- 本功能不采集新的个人敏感信息，不以埋点成功作为报名成功条件。

**追溯：**SCN-01、SCN-02；MET-01、MET-02、MET-03；AC-05。

## 4. 规则、数据与质量边界

### 4.1 业务规则

| 规则 ID | 规则 | 页面与系统结果 | 冲突优先级 |
|---|---|---|---|
| RULE-01 | 活动取消、结束或超过报名截止时间时，不再判断成员资格 | 返回 `closed`，按钮不可用，并展示具体最终状态 | 最高 |
| RULE-02 | 活动可报名且用户已报名时，不允许重复报名 | 返回 `registered`，展示已报名状态和结果入口 | 低于活动最终状态，高于资格阶段 |
| RULE-03 | 优先报名阶段按成员身份区分 `member_available`、`non_member_waiting` 和 `identity_unknown`；公开报名阶段返回 `normal_registration` | 页面说明、按钮状态和操作结果必须与返回状态一致 | 低于最终状态和已报名状态 |
| RULE-04 | 详情状态只用于展示，报名接口始终按点击时数据重新校验 | 若结果冲突，以报名接口结果为准，客户端展示原因并刷新详情 | 高于详情页旧状态 |

### 4.1.1 页面状态与操作矩阵

| 状态对象 / 区域 | 进入条件 | 用户看到什么 | 可执行操作 | 禁止或置灰操作 | 系统处理与数据结果 | 下一状态 | 关联规则 / 验收 |
|---|---|---|---|---|---|---|---|
| 报名区 | 活动已取消、结束或超过报名截止时间 | 最终状态提示，按钮不可用 | 查看活动信息或返回 | 报名 | 不继续判断成员资格 | 终止态 | RULE-01 / AC-04 |
| 报名区 | 用户已报名成功 | “你已报名”和结果入口 | 查看报名结果 | 重复报名 | 不创建重复报名 | 已报名态 | RULE-02 / AC-04 |
| 报名区 | 圈内优先报名阶段且用户为圈内成员 | 可优先报名说明，按钮可用 | 点击报名 | 无 | 点击时重新实时校验 | 报名成功 / 失败后刷新 | RULE-03、RULE-04 / AC-01 |
| 报名区 | 圈内优先报名阶段且用户不是圈内成员 | 等待公开报名说明，按钮不可用 | 等待或刷新 | 报名 | 不发起报名请求 | 公开报名后可报名 | RULE-03 / AC-02 |
| 报名区 | 成员身份无法确认 | 暂无法确认资格提示，提供刷新或报名校验入口 | 刷新资格 / 点击报名校验 | 展示肯定性可报名 | 报名时以实时校验结果收口 | 状态恢复 / 业务失败 | RULE-03、RULE-04 / AC-03 |

### 4.2 异常与边界

- 未登录用户不请求个体资格状态；点击报名时先进入登录流程，登录成功后重新请求详情，不复用登录前页面状态。
- 详情整体加载失败时不展示报名控制，避免在活动和资格信息不完整时提交。
- 仅成员身份失败时使用 `identity_unknown`，不得把未知身份当作圈内或圈外；用户可以刷新，也可以提交实时报名校验。
- 报名请求网络失败或超时时，不假定报名失败或成功；页面提示结果未确认，按钮恢复后允许重试，重新进入页面时通过详情状态确认最终结果。
- 连续点击由按钮加载状态拦截，同一个报名请求不得重复创建报名记录或重复上报结果事件。
- 旧客户端忽略新增展示字段，服务端报名校验和数据结果保持不变。

### 4.3 数据与埋点

| 字段 / 事件 | 定义与取值 | 生成 / 触发时机 | 使用规则 | 历史与兼容 | 数据源 |
|---|---|---|---|---|---|
| `eligibility_display_state` | `closed`、`registered`、`member_available`、`non_member_waiting`、`identity_unknown`、`normal_registration` | 每次成功返回圈子活动详情时重新计算 | 客户端只按枚举渲染；未知枚举按 `identity_unknown` 处理并记录错误 | 新字段不回填，旧端忽略 | 活动详情服务 |
| `priority_eligibility_hint_view` | 资格说明实际可见 | 指定三类提示进入可见区域 | 同一 `page_view_id`、同一状态只记一次 | 新事件，不回填 | 客户端埋点 |
| `activity_signup_click` | 一次有效报名点击 | 按钮进入加载态并准备发请求时 | 保留既有事件名和原有公共字段，新增 `page_view_id`、`eligibility_display_state`、`request_id` 等参数；加载期间重复点击不记录 | 既有事件不回填，新参数仅对新版本生效 | 客户端埋点 |
| `activity_signup_result` | 报名成功、业务失败或网络失败 | 收到一次请求的最终结果时 | 保留既有事件名和原有结果字段，新增 `page_view_id`、`eligibility_display_state`、`request_id`、`failure_code`；同一 `request_id` 只记一次 | 既有事件不回填，新参数仅对新版本生效 | 客户端、报名服务 |

### 4.4 非功能与质量要求

| 维度 | 要求 / 阈值 | 适用范围 | 验证方式 |
|---|---|---|---|
| 性能 | 新增状态不得增加额外串行请求；详情接口 P95 增量不超过 30ms | 活动详情 | 灰度监控 |
| 可访问性 | 提示文本可被读屏读取，不只依赖颜色表达资格 | UI-01、UI-02 | 客户端验收 |
| 可观测性 | 展示状态缺失率和接口错误率可按版本查看 | 灰度期 | 数据看板 |

## 5. 影响、依赖与发布

### 5.1 联动影响与兼容

| 影响面 | 结论 | 具体变化 | 历史或旧版本处理 | 负责人 |
|---|---|---|---|---|
| 报名资格规则 | 无影响 | 只增加展示状态 | 继续实时校验 | 服务端 |
| 管理后台 / 消息 / 搜索 | 无影响 | 无新增能力 | 不适用 | 产品确认 |
| 埋点 | 有影响 | 新增展示事件和参数 | 历史数据不回填 | 数据 |
| 运营客服 | 有影响 | 更新 FAQ 和截图 | 旧 FAQ 下线 | 运营 |

### 5.2 依赖

| 依赖项 | 前置产物 | 负责人 | 最晚时间 | 阻塞影响 | 状态 |
|---|---|---|---|---|---|
| 公开报名时间统一字段 | 接口字段确认 | 服务端 | 开发前 | 影响具体时间文案 | 待确认 |
| 原型 UI-01、UI-02 | 已确认 Frame | 设计 | 开发前 | 阻塞客户端实现 | 已完成 |

### 5.3 发布方案

- 先灰度 10% 圈子活动详情流量 2 天，再逐步放量。
- 监控详情接口错误率、状态缺失率、报名成功率和提示曝光覆盖率。
- 报名成功率较基线下降超过 5% 或接口错误率增加 0.2 个百分点时停止放量。
- 回滚时关闭展示配置，不改变服务端报名资格校验。

## 6. 验收与追溯

### 6.1 端到端追溯矩阵

| 目标 ID | 场景 ID | 功能 ID | 原型 ID / 节点 | 规则 ID | 验收 ID | 状态 |
|---|---|---|---|---|---|---|
| OBJ-01 | SCN-01 | FR-01、FR-02 | UI-01、UI-02 | RULE-01 至 RULE-04 | AC-01 至 AC-04 | 原型已确认 |
| OBJ-01 | SCN-02 | FR-01 | UI-03 / 报名校验节点 | RULE-03、RULE-04 | AC-03 | 流程图评审中，不阻塞接口开发 |
| OBJ-01 | SCN-01、SCN-02 | FR-03 | 不适用 | 不适用 | AC-05 | 事件待数据评审 |

### 6.2 研发验收标准

#### AC-01 圈内成员看到可执行的优先报名状态

- **Given：**活动处于圈内优先报名阶段、未关闭，登录用户为圈内成员且尚未报名。
- **When：**用户进入活动详情页，详情请求成功返回 `member_available`。
- **Then：**资格区展示“当前为圈内优先报名阶段，你可优先报名”，按钮显示“立即报名”且可点击；点击后按钮进入加载态并只提交一次请求。报名成功进入结果页，返回详情后展示 `registered`。

#### AC-02 圈外用户得到明确的等待条件

- **Given：**活动处于圈内优先报名阶段、未关闭，登录用户明确不是圈内成员且尚未报名。
- **When：**用户进入详情页，详情返回 `non_member_waiting`。
- **Then：**有公开报名时间时展示具体时间，无时间字段时展示不含具体时间的兜底文案；按钮显示“等待开放”、不可点击，且不产生报名请求。进入公开报名阶段并刷新后，状态切换为 `normal_registration`，按钮变为“立即报名”。

#### AC-03 未知身份和实时结果能够明确收口

- **Given：**优先报名阶段的成员身份查询失败，或页面展示后身份、活动阶段、名额发生变化。
- **When：**详情返回 `identity_unknown`，用户刷新资格或点击报名；或者用户从其他可报名状态点击报名。
- **Then：**未知身份时页面不得展示“可优先报名”；刷新操作重新请求详情，报名操作由报名接口实时校验。业务失败时用户留在详情页并看到可理解的具体原因，页面自动刷新状态；网络失败时提示结果尚未确认、按钮恢复可点击，未收到成功结果前不得展示已报名。

#### AC-04 最终状态、已报名和非圈子活动不受资格提示干扰

- **Given：**分别准备活动已截止 / 已结束 / 已取消、用户已报名、非圈子活动三组数据。
- **When：**用户进入详情页。
- **Then：**最终活动状态优先展示对应“报名已截止”“活动已结束”或“活动已取消”，按钮不可点击；已报名用户展示“你已报名”和结果入口，不重复提交；非圈子活动不展示圈内资格说明，并继续使用普通活动的报名状态和报名接口。

#### AC-05 埋点可串联、可去重且不阻塞主流程

- **Given：**测试环境能够查看客户端事件，并可模拟埋点上报失败。
- **When：**同一次页面访问中展示资格提示、触发重复渲染、点击一次报名并收到结果；随后模拟埋点上报失败重复执行主流程。
- **Then：**同一 `page_view_id` 和展示状态只有一条曝光；一次有效点击对应一条带相同 `request_id` 的点击和结果事件；加载期间重复点击不产生额外事件；事件参数符合 FR-03，三类事件可串联。埋点失败时页面展示、报名请求和报名结果均不受影响。

## 7. 效果验证

| 指标 ID | 对应目标 | 类型 | 口径 | 基线 | 目标 / 阈值 | 周期 |
|---|---|---|---|---|---|---|
| MET-01 | OBJ-01 | 行为 | 圈外用户优先期报名点击中资格限制返回占比 | 41% | 下降 30% | 上线后 2 周 |
| MET-02 | OBJ-01 | 结果 | 资格相关客服工单周均量 | 32 条 | 下降 20% | 上线后 4 周 |
| MET-03 | OBJ-01 | 护栏 | 符合资格用户详情访问至报名成功率 | 上线前补数 | 不下降超过 3% | 上线后 2 周 |

按活动规模、入口和用户身份分层，排除大型运营活动变化。MET-01、MET-02 达标且 MET-03 稳定时全量保留；护栏受损时暂停放量并复盘展示与按钮状态。

### 输出前一致性审查

| 检查域 | 检查结果 | 处理说明 |
|---|---|---|
| 术语与角色 | 一致 | 全文统一使用“圈内成员”“圈外用户”“活动详情页”，未混用“会员”“非会员”等其他称呼 |
| 状态、动作与结果 | 一致 | FR-01 的展示状态、FR-02 的页面操作与 AC-01 至 AC-04 的期望结果相同；报名成功仍以实时校验结果为准 |
| 数值、单位与口径 | 一致 | 优先期、公开报名时间和效果观察周期使用相同时间口径；MET-01 至 MET-03 的分母和周期无冲突 |
| 追溯关系 | 一致 | OBJ-01、SCN-01、FR-01 至 FR-03、RULE-01、AC-01 至 AC-05 和 MET-01 至 MET-03 均有回链 |
| 事实状态 | 1 项待确认 | 公开报名时间字段尚未统一，保留为 8.1 待确认项，不在正文假定字段一定存在 |

## 8. 风险、待确认项、研发 spec 交接与附录

### 8.1 风险、假设与待确认项

| 类型 | 内容 | 是否阻塞 | 验证 / 缓解方式 | 负责人 | 最晚时间 |
|---|---|---|---|---|---|
| 待确认 | 公开报名时间字段是否统一 | 阻塞具体时间文案 | 接口评审；无字段则使用兜底文案 | 服务端 | 开发前 |
| 风险 | 用户把“可报名”理解为已有名额 | 不阻塞 | 不使用“已获得名额”，测试理解度 | 产品、设计 | 设计验收前 |
| 效果假设 | 提前解释资格能减少无效尝试 | 不阻塞 | 用 MET-01、MET-02 联合验证 | 数据 | 上线后 4 周 |

### 8.2 待研发 spec 补齐事项

本节只列需要研发方案继续补齐的实现事项，不反向覆盖 PRD 已确认的页面表现和业务规则。

| 类别 | PRD 已确认的产品能力 / 结果 | spec 需补齐事项 | 是否阻塞产品评审 | 是否阻塞研发实现 | 负责人 / 下一步 |
|---|---|---|---|---|---|
| 接口能力 | 详情页需要获得可渲染的资格展示状态；报名仍由点击时实时校验 | API 路径、方法、请求响应字段、错误码、状态枚举命名、鉴权和兼容策略 | 否 | 是 | 服务端输出接口 spec |
| 客户端兼容 | 新版本展示资格说明，旧版本报名能力保持不变 | 旧端字段忽略策略、未知状态兜底、版本灰度开关 | 否 | 是 | 客户端 / 服务端评审 |
| 埋点与监控 | 需要判断提示曝光、报名点击和报名结果是否串联 | 事件字段、去重实现、看板和告警细节 | 否 | 是 | 数据 / 客户端确认 |

### 8.3 附录与资料索引

| 资料 | 链接 / 路径 | 版本 / 日期 | 用途 |
|---|---|---|---|
| 需求定义 | `docs/requirements/activity-priority-define.md` | 2026-07-10 | 产品决策来源 |
| 原型与流程 | UI-01 至 UI-03，见 2.2 | 2026-07-12 | 页面和交互主源 |
| 技术方案 | `docs/tech/activity-eligibility-state.md` | 待产出 | 接口、兼容、埋点和监控实现；不覆盖 PRD 规则 |
