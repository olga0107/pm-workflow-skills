interface:
  display_name: "pm-prototype-deliver"
  short_description: "把已确认方案生产为高保真可交互原型并发布交付"
  default_prompt: "使用 $pm-prototype-deliver 将这份已确认的方案做成可交互高保真原型：先完成开工三检查（参照物/设计技能/真实数据），接入真实数据后走通核心流程，按五通道打磨视觉与文案，最后发布为可访问链接并回写对齐文档。"

policy:
  allow_implicit_invocation: true
