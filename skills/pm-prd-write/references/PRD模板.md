# PRD 模板

本模板默认用于标准版 PRD。它消费已经确认的产品需求定义，只摘要和追溯背景、目标、方向与一级范围，不重新做产品决策。章节数量保持稳定；复杂需求通过补充矩阵和子项增加深度，不再增加大量平级章节。内部 MVP、单页复杂状态、异步任务或跨系统能力必须进入组合输出结构判断；缺少按条件应填的评审简版、页面状态与操作矩阵、系统能力需求表、研发 spec 交接清单或验收矩阵时，必须说明省略原因。

本模板同时服务四个目标：一是让需求、范围、流程、功能、规则和验收足够完备；二是让读者先理解整体路径和页面变化，再进入细节；三是明确 PRD 与研发 spec 边界；四是对需要页面体验评审的需求，先形成轻量的原型范围与页面状态规划，再交给 `pm-prd-html` 生成按阅读任务重组、完整规格可按需回看、带固定侧栏和黑白灰线框 SVG 的单文件 HTML。产品动线和页面信息结构只在会影响体验评审或实现完整性的需求中展开，不为所有需求强制增加文档负担；接口、队列、存储、算法等只写到产品能力和验收结果，具体实现进入 spec 交接清单。不要因为不写 spec 细节就把 PRD 压缩成方向、范围和功能清单；用户可见状态、操作限制、业务规则、异常兜底和验收标准仍必须具体。

**推荐阅读顺序：**先读第 0 章的一页结论，再读第 2 章的方案闭环、产品动线、页面信息结构和原型范围，再进入第 3、4、6 章核对具体行为、规则和验收。每个章节先写“本节结论”，再写表格或清单；表格承载事实，不能代替对行为的解释。

## 目录

0. 文档信息与一页结论
1. 决策依据
2. 方案总览、体验结构与原型
3. 交付范围与功能需求
4. 规则、数据与质量边界
5. 影响、依赖与发布
6. 验收与追溯
7. 效果验证
8. 风险、待确认项、研发 spec 交接与附录

## 章节适用性

| 章节 | 级别 | 何时需要 | 不适用时如何处理 |
|---|---|---|---|
| 0. 文档信息与一页结论 | 必填 | 所有需求 | 不可省略，可按轻量版压缩 |
| 1. 决策依据 | 必填 | 所有需求 | 不可省略，只摘要已确认决策 |
| 2. 方案总览、体验结构与原型 | 条件必填 | 改流程、页面、交互、多角色或多系统协作 | 整章省略，在 0.1 写明原因；简单实现说明可放入功能明细 |
| 3. 交付范围与功能需求 | 必填 | 所有需求 | 不可省略 |
| 4. 规则、数据与质量边界 | 条件必填 | 新增或改变状态、权限、规则、数据、异常处理或非功能风险 | 整章省略，在 0.1 写明原因 |
| 5. 影响、依赖与发布 | 条件必填 | 跨模块 / 系统、旧版本、历史数据、依赖、灰度、迁移或特殊发布 | 整章省略；常规发布可在 0.1 简述 |
| 6. 验收与追溯 | 必填 | 所有需求 | 验收不可省略；单功能可不用完整追溯矩阵 |
| 7. 效果验证 | 条件必填 | 需要判断用户行为、业务结果、实验效果或护栏变化 | 省略并说明为什么只需研发验收，例如恢复既定行为的缺陷修复 |
| 8. 风险、待确认项、研发 spec 交接与附录 | 可选 | 存在风险、假设、未决问题、待研发 spec 补齐事项或补充材料 | 无内容时整章省略，资料可合并到 0.1；内部 MVP 或跨系统能力建议保留 spec 交接清单 |

“不适用”和“信息缺失”不是一回事：应当适用但尚未拿到原型、规则、指标或上线结论时，必须保留对应章节并标记负责人、时间和是否阻塞。

## 组合输出结构（内部 MVP / 单页复杂状态 / 异步 / 跨系统判断）

当需求属于内部 MVP、异步任务、跨系统能力，或属于“单页多信息区且涉及状态流转、主操作、权限差异、异步结果、跨系统能力或关键异常兜底”时，必须进入组合结构判断；按下表决定哪些模块必填：

| 模块 | 放置位置 | 省略规则 |
|---|---|---|
| 评审简版 | 标准 PRD 前 | 内部 MVP 或评审会议不得省略；若用户只要正式 PRD，在 0.1 说明 |
| 标准 PRD | 第 0-8 章 | 默认不得省略 |
| 页面状态与操作矩阵 | 第 4 章或功能明细内 | 有状态流转、按钮可用性、权限或失败态时不得省略 |
| 系统能力需求表 | 第 3 章功能清单后 | 涉及接口、异步任务、第三方、存储、算法或跨系统时不得省略 |
| 研发 spec 交接清单 | 第 8 章 | 涉及接口 / 任务 / 存储 / 算法 / 第三方能力时不得省略 |
| 验收矩阵 | 第 6 章 | 标准 PRD 必须；轻量单功能可压缩但要说明 |

边界说明：页面状态与操作矩阵是跨功能、跨信息区的总览主源；功能明细中的条件分支只解释该功能内部行为，不要在两个位置重复粘贴同一组状态分支。系统能力需求表回答“产品需要系统做到什么、失败时用户看到什么”；研发 spec 交接清单回答“研发还要定义哪些接口、任务、存储、算法和监控细节”，不要把同一内容复制两遍。

## ID 与追溯规则

核心对象使用稳定 ID：目标 `OBJ-01`、场景 `SCN-01`、功能 `FR-01`、原型或设计 `UI-01`、规则 `RULE-01`、验收 `AC-01`、指标 `MET-01`。

涉及页面或流程时，可按需增加动线 `FLOW-01`、页面 `PAGE-01` 和信息结构 `IA-01`。Page 表示路由或页面容器；Section / 模块 / 信息区用 IA 表达；Flow 表示用户路径。单页多模块项目只建立一个 PAGE，再列多个 IA / Section，避免把模块误拆成多个页面。推荐形成：`目标 → 场景 →（动线 / 页面 / 信息结构，按需）→ 功能需求 → 原型/设计（如涉及界面）→ 规则 → 验收 → 指标`。不要为了填满追溯链而虚构 ID。ID 一经评审使用，不因章节移动而重编号。

## 评审简版（内部 MVP / 评审会议按需输出）

控制在 1-2 页，面向快速对齐，不复制完整 PRD。建议包含：

- 一句话方案：说明对象、场景、核心能力和结果。
- 目标与非目标：保留关键边界，避免反复扩写。
- 体验结构：明确是几个页面、页面内有哪些信息区 / 模块、主链路和删除 / 失败等关键分支。
- 核心规则：只列会影响用户体验或验收的规则。
- 验收标准：列 MVP 成功必须满足的端到端结果。
- 待研发 spec 补齐：列接口、队列、存储、算法、第三方能力等实现事项，不在简版中展开字段和方案。
- 省略说明：若组合输出结构中的必填模块被裁剪，说明省略原因和影响。

## 0. 文档信息与一页结论

### 0.1 文档信息

| 字段 | 内容 |
|---|---|
| 需求名称 |  |
| 文档状态 | 草稿 / 评审中 / 已确认 / 已关闭 |
| 负责人及参与角色 | 产品 / 设计 / 研发 / 测试 / 运营 / 数据 |
| 目标版本 |  |
| 创建与更新时间 |  |
| 需求定义来源 | `pm-requirement-define` 输出或等价决策记录 |
| 输出形态 | 标准 PRD / 评审简版 / 研发 spec 交接清单 / 内部 MVP 组合 |
| 关联资料 | 研究、数据、竞品、原型、技术方案、历史需求 |
| 省略章节及原因 | 例如：第 2 章不适用，本次为纯服务端规则修复，无页面和流程变化 |

### 0.2 一页结论

本节给不熟悉背景的读者快速建立判断，不写目录式短语。每条至少说明主语、条件、动作 / 变化和结果；不适用或尚未确认的内容明确标记。

- 一句话方案：为谁、在什么场景、解决什么问题、采用什么方向、产生什么结果。
- 已确认目标与方案：
- 关键取舍与非目标：
- 本次交付范围与版本：
- 效果判断标准：
- 用户主路径与页面影响（如适用）：明确几个页面，单页时说明页面内几个信息区。
- 关键信息结构变化（如适用）：区分 Page、Section / 信息区、Flow 和 IA。
- PRD / spec 边界：已写入 PRD 的产品能力，以及交给研发 spec 的实现事项。
- 最大风险或待决策项：

### 0.3 修订记录

| 日期 | 版本 | 变化或决策 | 原因 | 负责人 |
|---|---|---|---|---|

## 1. 决策依据

本章只固化上游决策。详细证据链接到需求定义、研究、竞品或数据材料；上游没有方案比较记录时，不在 PRD 中补做比较。

### 1.1 问题、目标与成功假设

| 问题与证据摘要 | 目标 ID / 目标状态 | 目标人群与场景 | 方向级成功假设 |
|---|---|---|---|

### 1.2 已确认方向、范围与取舍

- 已确认方向和核心思路：
- 选择依据及上游已记录的未选方向：
- 一级范围：涉及产品端、业务域、主流程和主要角色。
- 非目标与保持不变的原则：
- 重大约束：会影响可行性、投入量级或实现原则的约束。

### 1.3 关键决策记录

只记录会影响实现或验收的关键决策，不复制全部讨论过程。

| 决策项 | 已确认结论 | 依据 / 来源 | 决策人 | 日期 |
|---|---|---|---|---|

若详细设计必须改变目标、方向、一级范围或重大取舍，暂停规格展开并返回 `pm-requirement-define`。

## 2. 方案总览、体验结构与原型

### 2.1 方案闭环与目标流程

用一段话先说明方案如何从入口到结果形成闭环，再用表格核对每一步。每一行都写清楚谁在什么时间点接收什么输入、完成什么动作、得到什么反馈，以及下一步或终止条件；不要只写“处理数据”“进入下一步”。如果存在失败、返回、取消、重试或状态过期，至少在对应行写出用户回到哪里、看到什么、能否继续。

| 场景 ID | 阶段 / 时间点 | 角色 / 系统 | 前置输入 | 动作 | 系统反馈 | 下一步 / 临界点 |
|---|---|---|---|---|---|---|

### 2.2 产品动线与页面影响（按需）

当用户需要跨页面完成任务，或入口、页面跳转、返回、失败、取消、重试发生变化时，补充本节。重点说明“谁为了什么，从哪里进入，经过哪些页面或状态，到达什么结果”。如果是单页内完成任务，应明确写“单页内状态 / 信息区切换”，不要写成多页面跳转。

| 动线 ID | 场景 ID | 入口 / 起点 | 用户目的与关键动作 | 页面 / 状态转移 | 关键分支 | 结果 / 出口 | 返回、失败、取消或重试 |
|---|---|---|---|---|---|---|---|
| FLOW-01 | SCN-01 | PAGE-01 或业务入口 |  |  |  |  |  |

页面动线、页面内交互和业务状态流可以分别表达；不要求每份 PRD 同时输出三张图，也不要求使用 Mermaid。表格或文本路径可作为主源，流程图仅用于帮助快速阅读。页面转移不要只写页面名称，要说明触发动作、离开前状态、到达后用户要完成的任务，以及失败或返回后的落点。单页多信息区场景只建一个 PAGE，区块和状态变化放入 IA 或状态矩阵。

#### 页面地图与导航契约（多页面、跨页状态或返回语义复杂时必填）

| 页面 ID | 页面职责 / 所属壳层 | 入口与进入条件 | 离开条件 / 出口 | 返回 / 取消落点 | 跨页传递或保留的用户可见状态 | 关联动线 |
|---|---|---|---|---|---|---|
| PAGE-01 |  |  |  |  |  | FLOW-01 |

只记录影响用户路径和评审的导航事实，不写路由代码、参数名或前端缓存实现。共享导航、全局弹层或跨页对象未确认时标记待确认，不能由下游原型猜测。

### 2.3 页面信息结构（按需）

当新增页面、页面信息重组、核心信息前置 / 后置或页面状态变化会影响需求评审时，补充本节。先给出页面层级或结构摘要，再列关键区域和信息项，不重复原型已经清楚的视觉细节。严格区分页面容器与页面内信息区；用户说“一个页面里几个模块”时，模块写为 IA / Section，不新增 PAGE。

#### 页面结构摘要

```text
PAGE-01 页面名称
├── 核心信息区
├── 主任务 / 操作区
└── 辅助信息与状态区
```

#### 关键区域与信息项

| 页面 ID | 区域 / 信息结构 ID | 区域目的 | 关键内容或元素 | 优先级 | 展示条件 / 状态 | 操作结果 | 数据 / 规则来源 |
|---|---|---|---|---|---|---|---|
| PAGE-01 | IA-01 |  |  | P0 / P1 |  |  |  |

若页面信息结构不变，写明“页面信息结构不变”，并只列本次新增、删除、重组或状态变化的区域。信息结构不是视觉稿的坐标清单；每个区域至少说明它帮助用户理解什么、在什么条件下出现、与哪个操作或结果相关。

#### 复杂组件行为表（列表、表单、筛选、分页、批量、编辑或长数据时必填）

| 组件 / 区域 | 用户任务 | 默认内容与数据边界 | 交互状态 | 加载 / 空 / 错误 | 权限 / 校验 / 冲突 | 结果与恢复 | 关联规则 / 验收 |
|---|---|---|---|---|---|---|---|
| IA-01 |  |  |  |  |  |  |  |

只写用户可见的数据规模、长文本、选择、校验、分页或批量结果；接口字段、虚拟列表、表单库、缓存和实现策略交给研发 spec。

### 2.4 原型范围与页面状态规划（涉及页面体验时）

本节是产品逻辑到视觉交付的桥接层。只说明哪些页面 × 状态需要被可视化、每个原型状态要表达什么，以及生成原型需要遵守的产品和现实约束；不重复功能明细、业务规则或 SVG 代码。确认后的 PRD 可交给 `pm-prd-html` 生成一份按阅读任务重组、完整规格可按需回看、带固定侧栏和黑白灰线框 SVG 的单文件 HTML。

#### 原型适用性

- 是否需要原型：是 / 否 / 部分需要；
- 判断理由：是否改变页面、用户路径、状态、主操作或异常恢复；
- 不生成原型时的原因，以及是否仍需要流程图或结构化 HTML。

#### 页面 × 状态清单

| 原型状态 ID | 页面 ID | 状态名称 | 用户任务 | 需要表达的信息 / 反馈 | 主操作 | 下一状态 | 视口锚点（移动端） | 是否独立原型 | 关联功能 / 规则 / 验收 |
|---|---|---|---|---|---|---|---|---|---|
| UI-STATE-01 | PAGE-01 |  |  |  |  |  | 页面顶部 / 某信息区 / 弹层 | 是 / 否 | FR-01 / RULE-01 / AC-01 |

独立原型判断：用户看到的信息、页面结构、主操作、可用性、异常出口或角色权限发生实质变化时，通常需要独立画面；只有轻微选中态、禁用态或文案差异时可以合并，但必须在状态说明中写清。

#### 状态架构（多个独立状态域、并行异步或大量组合时必填）

| 状态域 | 所属层级 | 互斥 / 独立 | 进入与退出条件 | 优先级 / 组合限制 | 用户可见影响 | 覆盖方式 |
|---|---|---|---|---|---|---|
| 页面加载 | 全局 / 页面 / 信息区 / 组件 |  |  |  |  | 原型 / 流程 / 规则 |

同一状态域内的互斥状态可作为原型故事切换；独立状态域只画用户任务需要的代表组合，并在原型覆盖计划说明未单独成图的组合由哪条规则约束。不要把“角色 × 加载 × 编辑 × 权限 × 异步结果”无差别展开为全部截图。

#### 共享页面骨架与状态差异（单页多信息区时必填）

页面状态表回答“这一状态有什么不同”，本表回答“所有状态共同组成的页面是什么”。两张表不能相互替代。

| 信息结构 ID | 页面区域 | 所有状态保持的信息 | 随状态变化的信息 | 条件显示 / 隐藏 | 主操作或结果 | 原型表达要求 |
|---|---|---|---|---|---|---|
| IA-01 |  |  |  |  |  |  |

填写规则：

- 至少覆盖页面信息结构中所有 P0 区域；某状态不展示时写明隐藏条件和用户原因；
- 区分静态信息、动态数据、条件信息和操作反馈，不要只填内部状态名；
- `pm-prd-html` 以此表建立共享页面骨架，在各状态替换变化区域；页面较长时允许使用可滚动长页面或结构化缩略，不得因设备视窗限制删除 P0 信息区；
- 信息区展示条件未确认时标记待确认，不能由下游原型生成阶段猜测。

#### 移动端视口与滚动约束（移动端页面必填）

本节提供给 `pm-prd-html` 判断真实用户看到的页面范围，不写 SVG 坐标、CSS 或视觉稿细节。

| 约束项 | 需要确认的产品事实 |
|---|---|
| 端类型与逻辑视窗 | 例如 iOS / Android / 微信 H5，以及已确认的逻辑宽高；未确认时写目标设备范围和待确认项 |
| 页面长度与滚动 | 单屏、可滚动长页、分页或弹层；P0 区域在页面中的顺序和用户是否需要滚动 |
| 固定区域 | 顶部导航、吸底主操作、悬浮反馈等是否存在，以及何时出现 / 隐藏 |
| 状态视口锚点 | 用户进入该状态时，最需要看到的页面区域；写“页面顶部 / 录音区 / 结果区 / 弹层”等人类可读位置，不写像素坐标 |
| 信息密度与可访问性 | 目标用户的阅读、弱网、弱设备或无障碍约束；是否要求大字号、少操作、长文案换行或更大触控区域 |

长页的 P0 信息区必须在可滚动原型中完整可达，不要求同时出现在一个手机视窗；状态锚点应反映用户执行该动作后的真实停留位置，不能为了覆盖全页而缩小字号。

#### 端与布局矩阵（多端、响应式或端间交互差异时必填）

| 端 / 布局形态 | 目标用户与主任务 | 共享规则 | 信息层级 / 布局差异 | 操作差异 | 需要独立原型 | 设计资产 / 待确认 |
|---|---|---|---|---|---|---|
| 移动端 / 桌面端 |  |  |  |  | 是 / 否 |  |

不要把移动端画面简单放大为桌面端；只有布局和交互确实一致时才可共用同一原型，并说明依据。

#### 原型覆盖计划（体验交付复杂度为“复杂”时必填）

| 场景 / 页面 | 状态域或代表组合 | 端形态 | 覆盖形式 | 覆盖理由 | 未单独成图的组合及规则依据 | 关联验收 |
|---|---|---|---|---|---|---|
| SCN-01 / PAGE-01 |  |  | SVG 原型 / 流程图 / 规则上下文 |  |  | AC-01 |

覆盖计划以用户任务和风险为依据，不以状态数量为依据；P0 主路径、关键权限差异、关键异常和会改变布局或主操作的组合必须可评审。

#### 原型约束

- 端类型与页面尺寸：
- 目标用户与信息密度约束：
- 需要复用的既有产品形态 / 组件：
- 现有产品截图或设计参考：
- 页面长度、滚动、固定区域和状态视口锚点：引用上一表；
- 不应暴露给用户的内部术语：
- 已确认的文案、主操作和异常兜底：
- 交给 `pm-prd-html` 的原型视觉基线：默认黑白灰低保真线框，只表达结构、用户可见文案和交互状态，不放解释性文本、内部状态码或追溯信息。

本节不写接口字段、数据库状态值、前端组件实现或颜色 / CSS 细节；这些属于研发 spec 或 `pm-prd-html` 的呈现执行层。

### 2.5 原型与设计资产索引

| 资产 ID | 类型 | 精确链接 / 文件路径 / Frame | 版本或更新时间 | 状态 | 负责人 | 覆盖场景 / 页面 / 功能 |
|---|---|---|---|---|---|---|
| UI-01 | Figma / 原型 / 设计稿 / 流程图 / 截图 |  |  | 草稿 / 评审中 / 已确认 / 已废弃 |  | SCN-01 / FR-01 |

关联要求：

- 外部原型链接应定位到具体页面、Frame 或 Node，不只放项目首页。
- 仓库内文件使用可定位的相对路径；多页文件同时写页码、画板或节点名称。
- 原型尚未完成时写明负责人、预计时间和是否阻塞研发，不虚构链接。若由 `pm-prd-html` 生成，资产类型写为“黑白灰内嵌 SVG 原型 / 流程图”，不使用 PNG。
- PRD 是页面逻辑结构、业务规则和验收的主源；已确认原型是视觉布局和交互表达的主源。两者冲突时记录待确认项，并更新其中一方后再确认。

### 2.6 场景—原型关联

| 场景 ID / 流程步骤 | 功能 ID | 原型资产与节点 | 关键交互 / 状态 | 备注 |
|---|---|---|---|---|

## 3. 交付范围与功能需求

### 3.1 交付范围与版本

| 版本 | 目标 | 包含功能 / 页面 / 端 | 暂不包含 | 验证重点 |
|---|---|---|---|---|

### 3.2 功能清单

| 功能 ID | 功能点 | 所属页面（如适用） | 对应目标 / 场景 | 优先级 | 所属版本 | 原型 ID | 依赖 |
|---|---|---|---|---|---|---|

### 3.2.1 系统能力需求表（跨系统 / 接口 / 异步任务必填）

从产品角度描述页面和流程需要系统具备什么能力，以及用户可见结果和失败兜底。不要写 API 路径、请求 / 响应字段、数据库表、队列实现或算法参数。

| 能力 ID | 系统能力 | 支撑的页面 / 流程 | 触发时机 | PRD 需要确认的产品结果 | 失败或不可用时的用户兜底 | 关联功能 / 规则 | spec 需补齐 |
|---|---|---|---|---|---|---|---|
| CAP-01 |  |  |  |  |  | FR-xx / RULE-xx | 接口字段 / 队列 / 存储 / 算法等实现细节 |

### 3.3 功能需求明细

本节是研发、测试和产品复核的共同规格源。先写行为，再挂 ID；不能只列字段、枚举和引用关系。功能项按“用户要完成的一个结果”组织，不按接口字段或单个状态机械拆分。读完单个功能后，读者应能回答：谁在什么条件下触发、页面和系统分别做什么、不同条件下结果是什么、失败后如何恢复、哪些行为保持不变、如何验收。

**核心功能详写要求**

- P0 功能不得只写一段摘要或一句“支持某能力”；必须展开为“需求结论、行为说明、条件与分支、异常与恢复、保持不变与边界、追溯”。
- 若 P0 功能没有复杂分支，也要写清触发前提、页面表现、操作可用性、系统结果、下一状态和验收关联。
- P1 / P2 功能可以按风险合并，但不能省略会影响验收的状态、权限、异常和恢复路径。
- 评审简版可以压缩功能明细；标准 PRD 和示例 PRD 必须体现可供研发、测试和产品逐条评审的产品行为规格。

PRD 中的接口 / 系统能力写法：写“页面需要获取活动详情”“提交后创建或替换有效投稿”“删除自己的投稿后软删除并触发重新合成”“页面查询合成状态并展示最新作品”。不要写 API 路径、HTTP 方法、请求 / 响应字段、错误码、微信 serverId 下载、转码混音、队列、存储路径、重试超时等；这些放入 8.2 研发 spec 交接清单。

正例：PRD 不写 API 字段，但要写“用户点击提交后，系统创建或替换有效投稿，投稿池展示我的记录，最新作品进入合成中；提交失败时保留本地录音并允许重试”。这类内容属于产品行为和验收结果，不能因 spec 边界而省略。

#### FR-01 功能名称

**需求结论**

用 2 至 4 句完整句子说明实现后的行为。至少包含角色、场景、触发、核心变化和最终结果。不要写成“前置条件：已登录”“系统处理：查询状态”这类字段名加短语。

**行为说明**

1. **触发与前提：**谁在什么页面或业务环节执行什么动作；必须满足哪些登录、权限、数据和业务状态条件；不满足时进入哪个明确分支。
2. **用户可见行为：**页面新增、删除或改变什么；内容出现在哪里、默认展示什么、哪些操作可用；操作后用户立即看到什么反馈。
3. **系统与数据行为：**系统根据哪些已确认信息判断；何时读取、计算、保存或刷新；结果如何提供给页面和后续流程；是否产生状态或数据副作用。只写能力和产品结果，不写接口字段、存储路径或算法实现。
4. **结果与后续路径：**成功后进入什么页面或状态；失败后停留在哪里、展示什么、能否重试；返回或取消时是否保留已输入内容。

**条件与分支**（存在多个角色、状态或条件组合时使用）

| 进入条件 | 用户看到什么 | 可执行操作 | 系统处理与数据结果 | 后续页面 / 状态 | 优先级或冲突规则 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

每行必须表达一个完整分支，不能写“同原逻辑”“正常展示”“按情况处理”。至少写出进入条件、用户看到的内容或状态、是否可操作、操作后结果和下一步。需要复用既有行为时，写明可定位来源，以及本次要求保持不变的具体表现。

**异常与恢复**（只写本功能可能发生且会影响用户或数据的异常）

| 异常条件 | 用户反馈 | 系统 / 数据处理 | 是否可重试及方式 | 记录与告警 |
|---|---|---|---|---|
|  |  |  |  |  |

**保持不变与边界**

- 明确本次不改变的页面、规则、数据和流程；
- 说明功能不适用的角色、端、业务类型或历史数据；
- 若存在待确认项，写明负责人、最晚时间和阻塞影响，不在正文中假装已经确认。

**追溯**

- 对应目标 / 场景：OBJ-01 / SCN-01；
- 所属页面 / 信息结构（如适用）：PAGE-01 / IA-01；
- 原型 / 设计：UI-01，具体 Frame 或节点；
- 关联规则：RULE-01；
- 对应验收：AC-01、AC-02。

复杂页面按需增加元素表，只列新增、变化或高风险元素。页面结构和字段表不能代替上述行为说明。

| 信息结构 ID | 元素 | 内容与数据来源 | 默认状态 | 展示条件 | 可操作条件 | 操作结果 | 校验 / 报错 |
|---|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |  |

#### 表达粒度示例

- **不合格：**“系统根据用户状态返回展示状态，异常时兜底。”读者不知道何时判断、有哪些输入、页面怎么表现、兜底是什么。
- **合格：**“登录用户每次打开详情页时，服务端根据活动阶段、成员身份和报名状态返回唯一展示状态；客户端只负责按状态渲染，不自行推断资格。身份无法确认时页面不展示‘可报名’，而是显示‘暂时无法确认你的报名资格，请刷新后重试’，并保留刷新入口；用户点击报名时仍由报名接口重新校验，失败时留在详情页展示具体原因并刷新状态。”
- **拆分原则：**“生成状态”“展示文案”“点击报名”如果共同决定用户是否能完成报名，应在同一功能链路中说明；只有埋点有独立触发、参数和验收目标时，才单列为数据需求。
- **一句话需求反例：**“用户可以提交录音，提交后进入投稿池并触发合成。”这只说明了能力名，缺少前提、按钮状态、防重复、成功反馈、失败恢复、替换规则和验收关联。
- **示范级表达方向：**“用户在完成录音并进入待试听状态后，可以点击提交。点击后按钮进入提交中并禁止重复点击；提交成功时，本次录音成为该用户在当前活动下唯一有效投稿，投稿池立即展示‘我的投稿’，最新作品区进入合成中；提交失败时保留本地试听结果，按钮恢复可点击，并提示失败原因或重试建议。若用户此前已有有效投稿，本次提交替换旧投稿，旧投稿不再参与下一版合成。”

## 4. 规则、数据与质量边界

只保留适用于本需求的表格；不适用时写明原因。三种以上角色、状态或条件组合优先使用矩阵。

### 4.1 业务规则

每条规则至少能独立读成一句“当……时，谁可以 / 不可以做什么，系统产生什么结果，并进入什么后续状态”。存在多个条件组合时使用表格，并写明冲突优先级；不要把规则只写成名词或接口字段。

| 规则 ID | 条件 / 角色 / 当前状态 | 允许或禁止的动作 | 处理结果 / 后续状态 | 冲突优先级 | 异常处理 |
|---|---|---|---|---|---|

必要时拆成状态矩阵、权限矩阵或规则决策表。

### 4.1.1 页面状态与操作矩阵（有状态流转时必填）

当页面存在三种以上状态、按钮可用性变化、角色权限差异或失败兜底时，必须用本矩阵说明。每行至少写清进入条件、用户看到什么、能做什么、系统结果和下一状态。

| 状态对象 / 区域 | 进入条件 | 用户看到什么 | 可执行操作 | 禁止或置灰操作 | 系统处理与数据结果 | 下一状态 | 关联规则 / 验收 |
|---|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |  |

### 4.2 异常与边界

- 空状态、加载失败和重复提交：
- 数据缺失、脏数据和接口失败：
- 过期、关闭、删除、撤回和并发冲突：
- 多端、旧版本和降级表现：

### 4.3 数据与埋点

只定义会影响展示、判断、验收或指标的产品数据口径；数据库表结构、接口字段名、字段类型和索引设计交给研发 spec。

| 字段 / 指标 / 事件 | 定义与取值 | 生成或触发时机 | 展示 / 计算规则 | 历史数据处理 | 数据源 |
|---|---|---|---|---|---|

### 4.4 非功能与质量要求

按风险选择性能、容量、稳定性、可访问性、安全、隐私、合规、国际化、审计和可观测性要求。写出可验证阈值或“不适用及原因”，避免只写“性能良好”“保证安全”。

| 维度 | 要求 / 阈值 | 适用范围 | 验证方式 |
|---|---|---|---|

## 5. 影响、依赖与发布

### 5.1 联动影响与兼容

| 影响面 | 有 / 无 / 待确认 | 具体变化 | 历史或旧版本处理 | 负责人 |
|---|---|---|---|---|

至少扫描：用户端与后台、审核与权限、消息通知、搜索分发、导入导出与报表、埋点日志、第三方或跨系统接口、运营客服、历史规则和旧客户端。

### 5.2 依赖

| 依赖项 | 前置产物 | 负责人 | 最晚时间 | 阻塞影响 | 状态 |
|---|---|---|---|---|---|

### 5.3 发布方案

- 上线前数据、配置、培训或客服准备：
- 灰度范围、放量条件和监控告警：
- 历史数据迁移或补偿：
- 新旧版本兼容与降级：
- 回滚条件、方式和责任人：

## 6. 验收与追溯

### 6.1 端到端追溯矩阵

| 目标 ID | 场景 ID | 功能 ID | 原型 ID / 节点 | 规则 ID | 验收 ID | 状态 |
|---|---|---|---|---|---|---|

界面需求缺少已确认原型时，应在状态中标记是否阻塞；纯规则、接口或后台任务可将原型列写为“不适用”。涉及页面或动线时，可在矩阵中增加 `FLOW-xx`、`PAGE-xx`、`IA-xx` 列；轻量需求不必强行增加完整矩阵。

### 6.2 研发验收标准

每个验收用例必须能够独立执行，不把关键前提、页面表现或结果隐藏在“见原型”“按规则处理”中。Then（结果）应写出用户看到什么、系统状态如何变化、后续是否允许重试或继续。

#### AC-01 用例名称

- Given：前置角色、数据和状态。
- When：用户或系统执行的动作。
- Then：页面、交互、数据、状态和联动结果；必要时引用 UI-01 和 RULE-01。

至少覆盖主流程、关键状态、权限差异、异常失败、历史回归和跨模块联动。

## 7. 效果验证

### 7.1 指标方案

| 指标 ID | 对应目标 | 类型 | 定义与计算口径 | 基线 | 目标 / 阈值 | 数据来源 | 周期 | 负责人 |
|---|---|---|---|---|---|---|---|---|

指标类型可包括结果、行为、过程和护栏指标。

### 7.2 验证与决策规则

- 验证对象、样本范围和对照方式：
- 观察窗口与排除因素：
- 达标、未达标和护栏受损时分别采取什么动作：
- 数据不足时如何补数、延长观察或停止判断：

## 输出前自检清单

| 检查项 | 是否必须 | 通过标准 |
|---|---|---|
| 是否有评审简版 | 内部 MVP / 评审会议必须 | 1-2 页内讲清目标、范围、页面结构、核心规则、验收底线和待 spec 补齐事项 |
| 是否说明 1 个页面还是多个页面 | 页面需求必须 | 明确 Page 数量、入口、主任务和是否跨页面 |
| 是否区分信息区 / 模块 | 单页需求必须 | 单页容器用 PAGE，区块用 IA / Section，不误拆页面 |
| 是否有页面状态与操作矩阵 | 有状态流转、按钮可用性、权限或失败态时必须 | 每行写清进入条件、用户看到什么、可执行操作、系统结果和下一状态；不与功能分支表重复粘贴 |
| 是否有系统能力需求表 | 跨系统、接口、异步任务、第三方能力、存储或算法协作时必须 | 从产品角度写能力、触发时机、用户可见结果、失败兜底和验收，不写字段和算法；不复制 spec 交接清单 |
| 是否有 spec 交接清单 | 涉及接口 / 任务 / 存储 / 算法 / 第三方能力时必须 | 清楚列出 spec 要补齐的 API、队列、存储、算法、监控等实现事项 |
| 是否有验收矩阵 | 标准版 PRD 必须 | 目标、场景、功能、页面 / 信息区、规则和验收能互相追溯 |
| 是否清理已确认项和待确认项 | 必须 | 已确认、假设、待确认、不适用分开；待确认写负责人、最晚时间和阻塞影响 |
| 是否避免写接口字段和算法细节 | 必须 | PRD 只写能力和产品结果；字段、错误码、队列、存储、算法进入 spec 交接 |
| 是否避免过度压缩 | 必须 | 没有只停留在方向、范围和功能清单；用户状态、操作限制、规则、异常和验收已展开 |
| 是否通过一致性审查 | 必须 | 角色、术语、状态、单位、规则、异常、验收和追溯 ID 无冲突；无法消解的冲突已标为待确认 |
| 派生输出是否仍与 PRD 一致 | 生成受众视图时必须 | 只裁剪和重组正式 PRD，没有新增规则、实现细节或不同口径 |

### 一致性审查记录

| 检查域 | 检查范围 | 发现的冲突 / 断链 | 处理结果 | 剩余待确认与阻塞影响 |
|---|---|---|---|---|
| 术语与角色 | 全文、原型、相关决策 |  |  |  |
| 状态、动作与结果 | 流程、FR、规则、异常、AC |  |  |  |
| 数值、单位与口径 | 指标、质量要求、时间、金额、容量 |  |  |  |
| 追溯关系 | 目标、场景、功能、原型、规则、验收、指标 |  |  |  |
| 事实状态 | 已确认、假设、待确认、不适用 |  |  |  |

本表只记录实际发现和处理结果。全部通过时可写“一致，无未解决冲突”，不要为了填表虚构问题。

## 8. 风险、待确认项、研发 spec 交接与附录

### 8.1 风险、假设与待确认项

| 类型 | 内容 | 是否阻塞 | 验证 / 缓解方式 | 负责人 | 最晚时间 |
|---|---|---|---|---|---|

### 8.2 待研发 spec 补齐事项

本节只列交接清单，不在 PRD 中替研发确定实现方案。区分“已由 PRD 确认的产品能力”和“spec 需要补齐的实现事项”，便于产品评审和研发设计分层推进。

| 类别 | PRD 已确认的产品能力 / 结果 | spec 需补齐事项 | 是否阻塞产品评审 | 是否阻塞研发实现 | 负责人 / 下一步 |
|---|---|---|---|---|---|
| 接口能力 |  | API 路径、方法、请求响应字段、错误码、鉴权、幂等策略 | 否 / 是 | 是 |  |
| 异步任务 |  | 队列选型、任务去重、重试超时、失败告警 | 否 / 是 | 是 |  |
| 第三方能力 |  | 授权、素材下载、回调、异常码映射 | 否 / 是 | 是 |  |
| 存储与算法 |  | 存储路径、文件命名、转码参数、混音算法、容量策略 | 否 / 是 | 是 |  |
| 测试与运维 |  | 接口契约测试、压测、日志、监控、回滚脚本 | 否 / 是 | 是 |  |

### 8.3 附录与资料索引

| 资料 | 链接 / 路径 | 版本 / 日期 | 用途 |
|---|---|---|---|

可包括需求定义、研究与数据、竞品材料、原型和设计稿、技术方案、接口说明、术语表和历史决策。技术方案和接口说明作为 spec 资料索引，不应反向覆盖 PRD 已确认的产品规则。
